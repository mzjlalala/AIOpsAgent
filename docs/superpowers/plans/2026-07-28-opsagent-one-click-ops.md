# 一键运维 Implementation Plan

**Goal:** `POST /ops/one-click` + Mock `auto_ops` + 前端一键 CTA。

## Tasks

1. MockLLM 增加 `auto_ops` scenario + 报告文案
2. API：`POST /ops/one-click`（schema + router + proxy）
3. 前端：主按钮一键运维；高级区折叠自定义
4. 单测 + README 一句说明
